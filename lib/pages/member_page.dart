import 'package:flutter/material.dart';
import 'package:flutterbuild/cookbook/custom_themes.dart';

class MemberPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: MyHomePage(title: 'Custom Themesr',),
    );
  }
}